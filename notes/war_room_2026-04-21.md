# War Room 2026-04-21

## Goal
Push beyond the current best public score around `896k` and search for routes that could plausibly create a large public jump rather than another tiny calibration.

## Team Verdict
- Data Scientist: CatBoost recursive is saturated; highest-value routes are public perturbation map, TabPFN compact, direct-v2 late-only, and only then CatBoost knobs.
- Business Analyst: failed public submissions mostly punished lower Revenue/tail shapes; first public probes should test upward 2024 Revenue and higher 2024 COGS ratio.
- Code Engineer: implemented direct-v2 late-only hybrid probes, logs-only because original direct-v2 gate is false.
- QA: biggest risk was soft submit gates; several scripts could export failed candidates into `dataset/`. These were hardened.

## Completed Experiments

### Residual Correction
- Run: `logs/20260421_151736_anchor_residual_correction_probe/`
- Result: rejected.
- Best `residual_logcb_s25_c08` lost to anchor by about `+1.9k` combined MAE on time-forward folds.
- Dataset copies were removed; run-local files remain for audit.

### Direct-v2 Late Hybrid
- Script: `make_direct_v2_late_hybrid_probe.py`
- Run: `logs/20260421_152531_direct_v2_late_hybrid_probe/`
- Result: logs-only.
- Generated 27 candidates, but none were published to `dataset/` because direct-v2 source gate is false.
- Lightest manual probes if ever needed:
  - `logs/20260421_152531_direct_v2_late_hybrid_probe/submissions/submission_probe_direct_v2_cut30_lateafter60_w10.csv`
  - `logs/20260421_152531_direct_v2_late_hybrid_probe/submissions/submission_probe_direct_v2_cut30_lateafter90_w10.csv`
  - `logs/20260421_152531_direct_v2_late_hybrid_probe/submissions/submission_probe_direct_v2_cut45_lateafter60_w10.csv`

### Public Perturbation Map
- Script: `make_public_perturbation_map.py`
- Run: `logs/20260421_152653_public_perturbation_map/`
- Result: generated controlled public probes in `dataset/`.
- Recommended first submissions:
  - `dataset/submission_public_probe_rev2024h1_up5.csv`
  - `dataset/submission_public_probe_cogs2024h1_floor87.csv`
  - `dataset/submission_public_probe_promo_windows_rev_up6.csv`
- Rationale:
  - Anchor 2024 COGS/Revenue total ratio is about `0.849`, below 2021 actual `0.902` and 2022 actual `0.872`.
  - Prior public failures suggest lowering tail Revenue is toxic, so upward Revenue probes are the cleanest sign test.

### Breakthrough CatBoost Sprint
- Script: `run_breakthrough_catboost_sprint.py`
- Run: `logs/20260421_152529_breakthrough_catboost_sprint/`
- Result: rejected.
- Tested Revenue feature decoupling and local CatBoost hyperparameter/seed variants around recencyexp20.
- No candidate beat the public anchor on selector or recent-weighted combined MAE.
- `rev_forecast_core_strict_recency20` only beat recent-tail but lost global/recent badly; accidental dataset export was removed and run-local file is audit-only.

### TabPFN
- Script: `run_tabpfn_recursive.py`
- Result: stopped as not practical on this local runtime.
- Top100/e8 consumed heavy CPU for about 20 minutes without a fold result.
- Top50/e2 smoke also consumed heavy CPU without a useful fold result.
- Script is now logs-only by default and safer, but TabPFN is not a 2-hour sprint route here unless CUDA execution becomes reliably fast.

## QA Hardening Applied
- `run_leaderboard_sprint.py`: lazy-safe `statsmodels` import; removed fallback export when no candidate passes gate.
- `run_recency_weighted_sprint.py`: removed fallback export when no candidate passes gate.
- `run_public_revenue_direct_horizon_v2.py`: recursive COGS history now uses frozen anchor COGS instead of `0.0`; removed export-all behavior when gate fails.
- `run_tabpfn_recursive.py`: logs-only by default, fixed combined MAE convention, added CUDA fallback, reduced smoke defaults, and avoided validation-fold feature-selection leakage.
- `run_breakthrough_catboost_sprint.py`: export rule tightened so tail-only wins do not publish.

## Submit Order
1. `dataset/submission_public_probe_rev2024h1_up5.csv`
2. `dataset/submission_public_probe_cogs2024h1_floor87.csv`
3. `dataset/submission_public_probe_promo_windows_rev_up6.csv`

## How To Interpret Public Scores
- If `rev2024h1_up5` beats 896k: public wants higher 2024 Revenue; next create smaller/larger 2024 uplift grid and combine with promo windows.
- If `cogs2024h1_floor87` beats 896k: public wants higher 2024 COGS ratio; next tune floor `0.86/0.87/0.88`.
- If `promo_windows_rev_up6` beats 896k: promo prior is understated; next isolate windows and add COGS ratio-preserving promo variants.
- If all three lose: keep anchor as final; remaining pure-model routes are unlikely to jump to 700k without new data or better public leakage signal.

## Public Feedback Update
- `submission_public_probe_promo_windows_rev_up6.csv`: `888100.36839`.
- `submission_public_probe_cogs2024h1_floor87.csv`: `898472.39191`.
- `submission_public_probe_rev2024h1_up5.csv`: about `896000`.

Interpretation:
- The winning signal is not broad 2024 Revenue and not broad COGS ratio.
- Public specifically rewards Revenue uplift on historical promo windows.
- Avoid broad COGS correction for now.

## Promo Follow-Up
- Script: `make_public_promo_followup.py`
- Run: `logs/20260421_173038_public_promo_followup/`
- Generated amplitude and isolation probes around the winning promo-window signal.

Recommended next submit logic:
- Submit `dataset/submission_public_probe_promo_windows_rev_up8.csv` first.
- If `up8` improves on `888100.36839`, submit `dataset/submission_public_probe_promo_windows_rev_up10.csv`.
- If `up8` gets worse, submit `dataset/submission_public_probe_promo_windows_rev_up7.csv` to tune around the current best.
- After amplitude is bracketed, submit `dataset/submission_public_probe_promo_h1_rev_up8.csv` and/or `dataset/submission_public_probe_promo_h2_rev_up8.csv` to isolate which half-year carries the gain.

## TabPFN API Update
- Installed `tabpfn-client 0.2.8`.
- API direct probe script: `run_tabpfn_api_direct_probe.py`.
- Run: `logs/20260421_174159_tabpfn_api_direct_probe/`.
- Used `model_path="default"` because the client exposes explicit models `v2_default`, `v2.5_*`, and `default`, but no explicit `v2.6` model path in `TabPFNRegressor.list_available_models()`.
- The API call worked quickly, but the direct TabPFN donor pulled Revenue down:
  - `tabpfn_api_promo_donor_w20`: revenue total ratio `0.9928`.
  - `tabpfn_api_promo_donor_w40`: revenue total ratio `0.9857`.
  - `tabpfn_api_promo_donor_w60`: revenue total ratio `0.9785`.
- This is opposite the public-winning signal (`promo_windows_rev_up8`), so TabPFN API outputs were removed from `dataset/` and kept run-local only.

## TabPFN API Optimized Residual/Uplift Sprint
- Script: `run_tabpfn_api_optimized_sprint.py`.
- Run: `logs/20260421_181237_tabpfn_api_optimized_sprint/`.
- Token handling: reads only `PRIORLABS_TABPFN_TOKEN` from env and uses `ServiceClient.authorize()` directly; no token is written to logs/config.
- Frame: TabPFN residual/uplift meta-model around the `recencyexp20` 896k anchor. No raw direct TabPFN full forecast is published.
- API model paths tested: `default`, `v2.5_default`, `v2.5_real`, `v2.5_small-samples`, `v2.5_low-skew`.
- OOF residual target: `log1p(actual Revenue) - log1p(anchor Revenue_pred)` on 2020-2022 folds.
- OOF residual MAE improved materially versus zero-correction:
  - zero correction: `0.232283`.
  - `default`: `0.130209`.
  - `v2.5_real`: `0.119124`.
  - `v2.5_low-skew`: `0.114698` best OOF residual.
- Published public candidates:
  - `dataset/submission_tabpfn_promo_windowmix_v1.csv`: window-specific TabPFN shape, promo mean uplift `+7.89%`, Revenue total ratio `1.02632`.
  - `dataset/submission_tabpfn_promo_shape_cal8.csv`: TabPFN-shaped promo uplift normalized to `+8%`, Revenue total ratio `1.02666`.
  - `dataset/submission_tabpfn_promo_shape_cal10.csv`: TabPFN-shaped promo uplift normalized to `+10%`, Revenue total ratio `1.03333`.
  - `dataset/submission_tabpfn_promo_2024heavy_cal.csv`: heavier 2024 promo calibration, Revenue total ratio `1.03120`.
  - `dataset/submission_tabpfn_promo_shape_cogs4.csv`: same Revenue as cal8 with promo COGS co-move `+4%`, COGS total ratio `1.01375`.
  - `dataset/submission_tabpfn_promo_shape_cogs8.csv`: same Revenue as cal8 with promo COGS co-move `+8%`, COGS total ratio `1.02750`.
- Safety checks passed:
  - non-promo Revenue unchanged for promo-only candidates.
  - candidate Revenue total ratios stay within `1.00-1.05`.
  - no raw direct TabPFN full submissions in `dataset/`.
  - `python -m py_compile run_tabpfn_api_optimized_sprint.py` passed.
- Recommended submit order after current public best `submission_public_probe_promo_windows_rev_up8.csv = 887225.99926`:
  1. `dataset/submission_tabpfn_promo_windowmix_v1.csv`
  2. `dataset/submission_tabpfn_promo_shape_cal8.csv`
  3. `dataset/submission_tabpfn_promo_shape_cal10.csv`
  4. `dataset/submission_tabpfn_promo_2024heavy_cal.csv`
  5. `dataset/submission_tabpfn_promo_shape_cogs4.csv`
  6. `dataset/submission_tabpfn_promo_shape_cogs8.csv`

### Public Feedback on TabPFN Optimized Sprint
- `submission_tabpfn_promo_windowmix_v1.csv`: `883183.19507`.
- `submission_tabpfn_promo_shape_cal8.csv`: `888962.79663`.
- Interpretation:
  - `windowmix_v1` is the new best and improves the previous best `promo_windows_rev_up8 = 887225.99926` by about `4042.8` MAE.
  - `shape_cal8` is rejected, so global TabPFN promo ranking is worse than the flat public-winning uplift.
  - The live signal is specifically window-specific TabPFN allocation: Mar-Apr / Jun-Jul / Aug-Oct / Nov-Jan target mix plus within-window shape.
  - Do not submit `shape_cal10`, `2024heavy`, or shape-based COGS variants unless public feedback later contradicts this.

### TabPFN Windowmix Follow-Up
- Script: `make_tabpfn_windowmix_followup.py`.
- Run: `logs/20260421_182343_tabpfn_windowmix_followup/`.
- Generated candidates around the successful `windowmix_v1`:
  - `dataset/submission_tabpfn_windowmix_scale095.csv`
  - `dataset/submission_tabpfn_windowmix_scale105.csv`
  - `dataset/submission_tabpfn_windowmix_scale110.csv`
  - `dataset/submission_tabpfn_windowmix_soft_v1.csv`
  - `dataset/submission_tabpfn_windowmix_sharp_v1.csv`
  - `dataset/submission_tabpfn_windowmix_junjul12_v1.csv`
  - `dataset/submission_tabpfn_windowmix_augoct10_v1.csv`
  - `dataset/submission_tabpfn_windowmix_novjan04_v1.csv`
  - `dataset/submission_tabpfn_windowmix_cogs2.csv`
  - `dataset/submission_tabpfn_windowmix_cogs4.csv`
- New submit priority:
  1. `dataset/submission_tabpfn_windowmix_scale105.csv`
  2. `dataset/submission_tabpfn_windowmix_scale095.csv` if scale105 is worse; otherwise `dataset/submission_tabpfn_windowmix_scale110.csv`.
  3. `dataset/submission_tabpfn_windowmix_soft_v1.csv`.
  4. `dataset/submission_tabpfn_windowmix_junjul12_v1.csv`.
  5. `dataset/submission_tabpfn_windowmix_augoct10_v1.csv`.
  6. `dataset/submission_tabpfn_windowmix_cogs2.csv` only if Revenue-only windowmix variants improve further.

### TabPFN v2.5-Only Windowmix Split
- Script: `make_tabpfn_v25_windowmix_candidates.py`.
- Run: `logs/20260421_182817_tabpfn_v25_windowmix_candidates/`.
- Purpose: isolate the explicit `v2.5_*` API paths from the previous ensemble, because `default` is not confirmed as v2.6.
- Generated:
  - `dataset/submission_tabpfn_v25ens_windowmix_v1.csv`
  - `dataset/submission_tabpfn_v25low_windowmix_v1.csv`
  - `dataset/submission_tabpfn_v25real_windowmix_v1.csv`
  - `dataset/submission_tabpfn_v25default_windowmix_v1.csv`
  - `dataset/submission_tabpfn_v25small_windowmix_v1.csv`
  - `dataset/submission_tabpfn_v25ens_windowmix_scale105.csv`
  - `dataset/submission_tabpfn_v25low_windowmix_scale105.csv`
  - `dataset/submission_tabpfn_v25ens_windowmix_soft.csv`
  - `dataset/submission_tabpfn_v25low_windowmix_soft.csv`
- Submit priority if explicitly testing v2.5:
  1. `dataset/submission_tabpfn_v25low_windowmix_v1.csv`
  2. `dataset/submission_tabpfn_v25ens_windowmix_v1.csv`
  3. `dataset/submission_tabpfn_v25low_windowmix_scale105.csv`
  4. `dataset/submission_tabpfn_v25ens_windowmix_soft.csv`

### Public Feedback on v2.5-Only
- `submission_tabpfn_v25low_windowmix_v1.csv`: `883881.53813`.
- This is worse than current best `submission_tabpfn_promo_windowmix_v1.csv = 883183.19507` by about `698.3` MAE.
- Interpretation: single explicit `v2.5_low-skew` is not better than the mixed API ensemble. Do not continue single v2.5 unless submission budget is abundant.

### True Local TabPFN 2.6
- Source: Hugging Face `Prior-Labs/tabpfn_2_6`.
- Checkpoint: `tabpfn-v2.6-regressor-v2.6_default.ckpt`.
- Script: `run_tabpfn26_local_residual_windowmix.py`.
- Run: `logs/20260421_183330_tabpfn26_local_residual_windowmix/`.
- Device: CUDA.
- OOF residual metrics:
  - zero correction all: `0.232283`.
  - TabPFN 2.6 residual all: `0.114720`.
  - TabPFN 2.6 residual promo: `0.117715`.
  - TabPFN 2.6 residual non-promo: `0.112838`.
- Generated:
  - `dataset/submission_tabpfn26_windowmix_v1.csv`
  - `dataset/submission_tabpfn26_windowmix_scale095.csv`
  - `dataset/submission_tabpfn26_windowmix_scale105.csv`
  - `dataset/submission_tabpfn26_windowmix_scale110.csv`
  - `dataset/submission_tabpfn26_windowmix_soft_v1.csv`
  - `dataset/submission_tabpfn26_windowmix_sharp_v1.csv`
  - `dataset/submission_tabpfn26_windowmix_junjul12_v1.csv`
  - `dataset/submission_tabpfn26_windowmix_augoct10_v1.csv`
  - `dataset/submission_tabpfn26_windowmix_cogs2.csv`
- Recommended first submit for true 2.6:
  1. `dataset/submission_tabpfn26_windowmix_v1.csv`
  2. If v1 improves current best, try `dataset/submission_tabpfn26_windowmix_scale105.csv`.
  3. If v1 worsens, try `dataset/submission_tabpfn26_windowmix_soft_v1.csv`.
