# Session Handoff - 2026-04-18

## Context
- Repo: `C:\Users\admin\Documents\Project\datathon`
- User status: currently around top 25 on the competition leaderboard, about `350k` worse than top 1.
- Main question this session: whether to chase new model families (`TFT`, `TabPFN`) or improve the current recursive `XGBoost` system.

## Current Best Known Baseline
- Current submission-track winner is still `system_promo_plus_cogs_blend60_clip_q99`.
- Source of truth:
  - `outputs/final/final_model_report.md`
  - `outputs/final/recursive_backtest_summary.csv`
  - `outputs/final/recursive_backtest_folds.csv`
- Mean recursive backtest metrics before any new unfinished changes:
  - Revenue MAE: about `746,706`
  - COGS MAE: about `568,137`

## Key Conclusions Reached
- The biggest likely source of leaderboard gap is still validation mismatch / future-feature mismatch, not a broken submission file.
- `future promo = 0` is a likely weak point for hidden test performance.
- `TFT` showed only a very small MAE edge on one screen fold and was much slower. It is not strong enough yet to replace XGB.
- `TabPFN` is worth treating as a challenger only, not as the main direction.
- Best expected-value direction remains:
  1. keep current XGB winner as frozen baseline
  2. improve future promo proxy
  3. add a narrow regime-aware fallback on top of XGB

## Completed Experiments
- Long-horizon selector run completed:
  - `logs/20260418_195029_long_horizon_selection/`
  - Result: long-horizon winner still remained `system_promo_plus_cogs_blend60_clip_q99`
- Revenue family screen completed on one fold:
  - `logs/20260418_203103_revenue_family_screen/`
  - `TFT` revenue MAE: about `783,989`
  - `XGB current winner` revenue MAE: about `787,689`
  - Interpretation: slight TFT signal on one fold, not enough evidence to switch families
- `TabPFN v2.6` run was started later but never completed:
  - partial run dir: `logs/20260418_204710_revenue_family_screen/`
  - weights for `v2.6` were downloaded successfully
  - run was stopped manually by user request

## Code Changes Made
- `run_revenue_family_screen.py`
  - added heartbeat/status support
  - switched TabPFN logic from hardcoded `v2.5` to auto-prefer `v2.6` when available
  - added clearer TabPFN heartbeat stages for future reruns
- `train_recursive_forecast.py`
  - added promo future policy framework:
    - `zero`
    - `seasonal_month_day_recent_2y`
  - added revenue regime framework:
    - `none`
    - `promo_heavy_switch`
  - added new challenger systems:
    - `system_promo_plus_cogs_md2y`
    - `system_promo_plus_cogs_md2y_revenue_switch`
  - wired promo future policy and optional revenue fallback into recursive forecasting path
- `run_long_horizon_selection.py`
  - updated to use the same promo future policy and revenue regime hooks as the main recursive pipeline

## Important Validation State
- Syntax check passed after edits:
  - `python -m py_compile train_recursive_forecast.py`
  - `python -m py_compile run_long_horizon_selection.py`
  - `python -m py_compile run_revenue_family_screen.py`
- Full backtest of the new challenger systems did **not** complete.
- `python train_recursive_forecast.py` was started, then interrupted by the user.
- All running Python processes were later killed. GPU is idle now.

## Most Useful Analytical Finding From This Session
- Simple promo priors were tested offline on historical promo features.
- `seasonal month-day recent 2 years` looked materially better than `zero-fill` for forecasting future promo signals.
- Narrow regime test on fold `2020` suggested:
  - `system_promo_only` is safer on promo-heavy days
  - current winner is better on non-promo-heavy days
- This is the direct rationale for the new `promo_heavy_switch` challenger.

## Recommended Next Step
- Do not resume `TFT` or `TabPFN` first.
- First run the updated XGB pipeline and compare:
  - current winner
  - `system_promo_plus_cogs_md2y`
  - `system_promo_plus_cogs_md2y_revenue_switch`
- Then rerun long-horizon selection on the same set.
- Because the user explicitly stopped scripts near the end of this session, ask before launching another long-running benchmark.

## User Preference To Preserve
- User asked to stop scripts. Do not start another long-running Python run without explicit confirmation first.

## Security Note
- A TabPFN API token was pasted into chat during this session.
- The raw token is intentionally not stored here.
- Best practice: revoke or rotate that token after the session.

## Useful Paths
- `outputs/final/final_model_report.md`
- `outputs/final/recursive_backtest_summary.csv`
- `outputs/final/recursive_backtest_folds.csv`
- `logs/20260418_195029_long_horizon_selection/report.md`
- `logs/20260418_203103_revenue_family_screen/report.md`
- `logs/20260418_204710_revenue_family_screen/status.json`
